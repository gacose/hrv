---
title: "Bitacora"
output: html_document
---

Este es un ejmeplo de como calcular la **desviaci贸n est谩ndar** <http://rmarkdown.rstudio.com>.



```{r}
a<- read.csv('../Datos/gc12.csv')
sd(a$X619)
```


Los archivos de los periodos de tiempo (tacogramas) entre latidos de cada persona fueron cambiados de formato, para poderlos cargar con distinta extensi贸n en R. En cada archivo quedan los 3 registros, la columna de 12 respiraciones por minuto como **a**, la columna de datos de 9 respiraciones por minuto como **b** y la de 6 respiraciones por minuto como **c**.
Para obtener la desviaci贸n estandar de las columnas de tacogramas, se revisa la cantidad de datos para delimitar desde y hasta donde debe hacer el c谩lculo.

```{r}
b<- read.csv('../Datos/gc.csv',header = TRUE)
#Calculo la desviaci贸n para 12 respiraciones

c1 <- sd(b$a[0:382])
c2 <- sd(b$b)
c3 <- sd(b$c[0:392])
lista=c(c1,c2,c3)
```
```
```{r}
table(lista)
```

```{r}
for(elemento in b){
 # x<-!is.na(elemento);
# sd(elemento[x]);
elemento
}
```



Este c贸digo me sirve para hacer el ciclo de realizar la desviaci贸n est谩ndar para cada archivo con las 3 columnas, arrojando los 3 resultados.

```{r}
for(elemento in 1:length(b)){
  x<-!is.na(b[elemento])
  y = b[elemento][x]
  print(sd(y))
}
```
Para Carlos Cecilio Pacheco c
```{r}
c<- read.csv('../Datos/ccp.csv',header = TRUE)
```
```{r}
for(elemento in 1:length(c)){
  x<-!is.na(c[elemento])
  y = c[elemento][x]
  print(sd(y))
}
```


Para Francisco Gonz醠ez d
 Edgardo Morales e
 Azucena Chavez f
 Sa鷏 Hern醤dez g
 Ra鷏 Alejandro h
 N閟tor Ort韟 i
 J Antonio G j
 FS Guzm醤 k
 Marco Vinicio l
```{r}
d<- read.csv('../Datos/fg.csv',header = TRUE)
e<- read.csv('../Datos/em.csv',header = TRUE)
f<-read.csv('../Datos/ach.csv',header = TRUE)
g<-read.csv('../Datos/sh.csv',header = TRUE)
h<-read.csv('../Datos/ra.csv',header = TRUE)
i<-read.csv('../Datos/no.csv',header = TRUE)
j<-read.csv('../Datos/jag.csv',header = TRUE)
k<-read.csv('../Datos/fsg.csv',header = TRUE)
l<-read.csv('../Datos/mv.csv',header = TRUE)


lista=c(d,e,f,g,h,i,j,k,l)


for(s in 0:length(lista)){
  temp<-lista[s]

  for(elemento in 0:length(temp)){
  x<-!is.na(temp[elemento])
  y = temp[elemento][x]
  print(sd(y))
}}
```


